.mypage
  .mypage__profile
    .mypage__profile--name
      =@user.nickname
      -if @user.image.url
        =image_tag @user.image.url, class: 'userimage', size: "150x150"
      -if user_signed_in? && @user == current_user
        = link_to "プロフィール編集", edit_user_path(current_user)
    .mypage__profile--contents
      .follower
        フォロワー
        人
      .followed
        フォロー
        人
  .mypage__posts
    投稿
    .index
      -@post.each do |post|
        .index-box
          =link_to post_path(post.id) do
            .index-image
              .mypage__contents--image
                =image_tag post.image.url, class: 'vocabimage', size: "250x250"
            .index-contents
              .index-contents__vocab
                =post.vocab
              .index-contents__definition
                =post.definition
              .index-contents__like
                =post.liked_users.count
              .index-contents__time
                =post.created_at.strftime("%Y年%m月%d日 %H時%M分")
        
  いいねした投稿
  .mypage__likes
    -@user.liked_posts.each do |post|
      = post.user.nickname
      = post.vocab
